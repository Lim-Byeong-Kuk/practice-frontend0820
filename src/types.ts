export interface ProductType {
  productId: number;
  name: string;
  explanation: string;
  price: number;
}

export interface ProductResponse {
  pno: number;
  title: string;
  content: string;
  writer: string;
}
